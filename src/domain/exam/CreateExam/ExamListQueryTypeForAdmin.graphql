
mutation getSubjectList($filter: ExamListFilterInput!) {
  getSubjectList(filter: $filter) {
   examName
   semester
    examDate
    startTime
    endTime
    gradeType
    total
    passing
    actions

  }
}