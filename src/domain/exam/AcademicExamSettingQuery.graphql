#import "../gqlfragments/academicexamsettingDetails.graphql"

query academicExamSetting($academicExamSettingId: Long!) {
  academicExamSetting(id: $academicExamSettingId){
  ...academicexamsettingDetails
  }
}
