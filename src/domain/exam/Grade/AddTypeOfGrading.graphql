mutation addTypeOfGrading($input: [AddTypeOfGradingInput!]!) {
  addTypeOfGrading(input: $input) {
       typeOfGrading {
      id
     minMarks
        maxMarks
        grades
       groupvalue
    }
  }
}

